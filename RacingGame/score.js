export class Score {
    /**
     * 
     * @param {number} points 
     * @param {Date} setAt 
     */
    constructor(points, setAt) {
        this.points = points;
        this.setAt = setAt;
    }
}